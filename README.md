# E-commerce Inventory System with AWS Serverless deployment

A serverless e-commerce inventory management system built with AWS Lambda, API Gateway, and DynamoDB.

## Setup

1. Install dependencies:
   ```bash
   npm install
